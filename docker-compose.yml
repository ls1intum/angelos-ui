services:
  angelos-ui:
    image: "ghcr.io/ls1intum/angelos-ui:latest"
    container_name: angelos-ui
    expose:
      - "80:80"
    networks:
      - angelos-network

networks:
  angelos-network:
    external: true
