services:
  angelos-ui:
    image: "ghcr.io/ls1intum/angelos-ui:latest"
    container_name: angelos-ui
    ports:
      - "80:80"
    expose:
      - "80"
    networks:
      - angelos-network

networks:
  angelos-network:
    external: true
